{
  "name": "validate",
  "task": "agency-environment/lib/tasks/validate",
  "config": {
    "subtasks": [{
      "name": "default",
      "files": {
        "src": ["<%= destination %>/**/*.html"],
        "dest": "<%= destination %>/w3c"
      }
    }]
  }
}
