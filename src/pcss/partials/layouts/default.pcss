html {
  box-sizing: border-box;
}

html, body {
  margin: 0;
  padding: 0;
  overflow-x: none;
}

*, *:before, *:after {
  box-sizing: inherit;
  -webkit-tap-highlight-color: rgba(0,0,0,0);
  -webkit-touch-callout: none;
}

.partial[data-partial="layouts/default"] {
  .no-select {
    user-select: none;
  }
}
